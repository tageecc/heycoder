# Scripts

- `update_readme_contributors.py` — Regenerates the overlapping avatar block in `README.md` (between `<!-- readme: contributors -->` markers). CI runs it on push to `main`; locally: `GITHUB_REPOSITORY=owner/repo python3 script/update_readme_contributors.py`.

Optional tooling for indexes or drafts from `content/` can also live here.
